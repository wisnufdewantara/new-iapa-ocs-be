node_modules
dist
# Keep environment variables out of version control
.env

/generated/prisma
SESSION_NOTES.md
CUTOVER_CHECKLIST.md

# Upload baru (Submit Paper dst) disimpan di disk server, bukan di-commit
/uploads/*
!/uploads/.gitkeep

# Lock file cron sync (scripts/sync-from-supabase.mjs), dibuat runtime
scripts/.sync.lock

# Claude Code — konfigurasi personal, nggak di-share lewat git
.claude/
